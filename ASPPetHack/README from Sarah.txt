DSN=sql;server=localhost;database=Pethack;

Trusted_Connection=True;server=localhost;Database=Pethack


Put Pethack MDF and Pethack_Log. LDF into directory sql server > mssql12.mssqlserver> DATA directory to see if can just open on your own machine.
Can try to hook up to visual studio. these are just the tables I did not put any data. I can try to put data if you are able to do these things on your machine but no point
if can't connect. Let me know